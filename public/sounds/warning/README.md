# public/sounds/warning

포어(FORE!) 등 안전 경고 사운드를 넣는 폴더입니다.
`category: "warning"` 항목은 카탈로그에는 등록되지만 GalleryPanel(응원 버튼 영역)에는 노출되지 않습니다 — 안전 경고는 응원과 섞이면 안 되므로, 별도의 경고 전용 UI에서 `getSoundCatalog()`로 조회해 사용하는 것을 권장합니다.
